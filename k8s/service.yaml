apiVersion: v1
kind: Service
metadata:
  name: mlids-api
  labels:
    app: mlids-api
spec:
  type: ClusterIP
  selector:
    app: mlids-api
  ports:
    - name: http
      port: 8000
      targetPort: http
